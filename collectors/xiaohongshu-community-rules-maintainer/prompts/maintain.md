# Maintain community rules

Use the Connector's read-only browser extraction contract on the configured official URL. Pass the rendered observation to the deterministic maintainer. If the page cannot render, any assertion changes, or the accepted verification expires, return proposals only. Never edit the accepted baseline, Connector, or canonical knowledge automatically.
