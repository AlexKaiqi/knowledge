# Contract eval

- Missing rendered observation requests one browser observation proposal.
- Same semantic digest and fresh verification return `current` with no proposals.
- Digest change, semantic assertion failure, render failure, or verification expiry returns `review-required` proposals without updating canonical files.
