# Bounded work context maintainer

This hidden Collector watches the accepted Personal Knowledge Base projection implementation, repository layout and production E2E evidence. It also checks expiry of the isolated local probe.

Every change becomes a proposal. Main-branch implementation or layout drift requests Connector review; pinned evidence drift remains a knowledge review. It never changes the public OKF, production plugin, Session history, current projection or durable knowledge automatically.
