# Duplex turn policy projector

Hidden deterministic Connector for `voice.project-duplex-turn-events-to-actions`. It consumes normalized, text-free duplex events and returns a reversible action plan. It does not receive raw audio, open a microphone, control playback, contact a Provider, or execute any projected action.
