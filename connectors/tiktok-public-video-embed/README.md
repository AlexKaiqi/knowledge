# TikTok public video embed Connector

Hidden deterministic Connector for TikTok's documented public oEmbed endpoint. It accepts one canonical public video URL and returns a minimized descriptor. It does not expose the author, upstream HTML, thumbnail URL, cookies, tokens, metrics, comments, or media bytes.

This Connector is not public keyword search, a crawler, an owned-account reader, or a posting client.
