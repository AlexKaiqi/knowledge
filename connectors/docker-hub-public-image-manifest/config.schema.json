{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dsh.dev/knowledge/connectors/docker-hub-public-image-manifest/config.schema.json",
  "title": "DockerHubPublicImageManifestConnectorConfig",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "timeoutMs": { "type": "integer", "minimum": 1000, "maximum": 60000, "default": 15000 },
    "userAgent": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+ \\((https://[^()\\s]+|[^()\\s@]+@[^()\\s@]+)\\)$"
    }
  }
}
