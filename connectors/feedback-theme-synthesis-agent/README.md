# feedback-theme-synthesis-agent

Hidden agentic Connector for `/capabilities/feedback/synthesize-feedback-theme-evidence.md`.

The public input is a caller-authorized, deidentified and bounded evidence sample. The Connector asks an Agent for candidate themes, then independently checks evidence references, derives sample counts, preserves counterexamples and gaps, and fixes all outputs as human-review-required and non-executing.
