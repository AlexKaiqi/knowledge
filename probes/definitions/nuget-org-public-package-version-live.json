{
  "schemaVersion": "dsh.probe-definition/v1",
  "id": "nuget-org-public-package-version-live",
  "version": "1.0.0",
  "capabilityRef": "/capabilities/nuget-org/read-public-package-version-evidence.md",
  "connectorId": "nuget-org-public-package-version",
  "environment": "production-public",
  "identity": { "required": false },
  "effect": "none",
  "steps": [
    { "id": "discover-v3-resources", "operation": "readPublicPackageVersionEvidence" },
    { "id": "read-registration-and-package", "operation": "readPublicPackageVersionEvidence" },
    { "id": "verify-downloaded-artifact", "operation": "readPublicPackageVersionEvidence" }
  ],
  "assertions": [
    "The NuGet.org V3 service index exposes SemVer 2 registration and package-content resources without authentication.",
    "Newtonsoft.Json 13.0.3 Registration identity, listed state, MIT expression and minimum client version match the reviewed fixture.",
    "The actual 2,441,966-byte nupkg SHA-256, SHA-512, ZIP entry count, central-directory digest, nuspec entry and signature entry match the reviewed fixture.",
    "The current China route is accepted only as a one-hop, same-path api.nuget.org to nuget.azure.cn transport redirect; no alternate product feed is accepted.",
    "The normalized output excludes authors, owners, dependency groups, vulnerability URLs and raw payload; the package is not installed, executed or dependency-resolved, and signature presence is not represented as cryptographic verification."
  ],
  "cleanup": [],
  "budget": { "timeoutSeconds": 60, "maxCost": 0, "currency": "CNY" }
}
