{
  "schemaVersion": "dsh.probe-definition/v1",
  "id": "osv-public-advisory-live",
  "version": "1.0.0",
  "capabilityRef": "/capabilities/osv/read-public-advisory.md",
  "connectorId": "osv-public-advisory",
  "environment": "production-public",
  "identity": { "required": false },
  "effect": "none",
  "steps": [{ "id": "read-known-advisory", "operation": "readPublicAdvisory" }, { "id": "verify-range-and-minimization", "operation": "normalizePublicAdvisoryResponse" }],
  "assertions": [
    "Official GET /v1/vulns/{id} returns OSV-2020-111 without authentication.",
    "The fixed poppler OSS-Fuzz GIT introduced/fixed boundaries and REPORT reference match reviewed evidence.",
    "The Connector rejects alternate hosts, redirects, malformed IDs and responses above 2 MiB, and does not retry errors.",
    "Credits, database/ecosystem-specific objects and raw payload are excluded; details and long version lists are bounded with hashes and explicit coverage."
  ],
  "cleanup": [],
  "budget": { "timeoutSeconds": 30, "maxCost": 0, "currency": "CNY" }
}
