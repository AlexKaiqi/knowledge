---
type: Log
title: Knowledge bundle history
---

# Bundle history

## 2026-08-31

- 补齐 Optifeed Radar 的远程可复现接入链：新增 pinned runtime bootstrap 与正式 domain audit CLI，默认只使用 Git 忽略的 `.runtime/optifeed-radar`，调用前后验证产品 Schema，不再依赖开发机兄弟目录。
- 实验准入 `distribution.audit-store-ai-readiness`：固定 Optifeed Radar `0.3.0` 源码 commit，以独立 clone 的已构建 core 对 bare public hostname 执行零密钥、零 AI 调用、零费用 readiness audit。隐藏 Connector 在每次请求与重定向前重新解析 DNS、拒绝内网/本地/保留地址并把连接固定到已验证公网 IP；公共结果只保留 readiness 分数、五类分项、发现项、crawler root access 和测量边界。`optifeed.com` live probe 返回 100/100、五个分项和九个 crawler 项；付费 visibility、shopping、buyer-query generation 和 snapshot diff 均未准入，100 分不冒充品牌/商品被 AI 推荐。

## 2026-08-27

- 实验准入 `assistant.observe-multi-turn-response-repetition`：按 case 与 locale 对有界助手回复执行规范化原样重复和 2/3-gram 历史重叠观测；确认复述、按请求重复、纠错、安全边界和口头禅仅保留调用方 provenance，不删除原始计数。中英文 9 条回复 fixture、公开 Schema、输入原文不留存和零副作用已通过真实本地 probe。能力没有阈值或质量分，不推断语义重复、Persona 连续性或长期陪伴结果；五源 proposal-only Collector 持续观察参考实现、论文语义与反证。

- 准入 `steam.prepare-initial-base-price-review-revision`：冻结未发布标准基础包、37 个 live currencies、四个 USD region groups、最小货币单位金额、调用方观察最低阈值、market/value evidence 与审核后发布模式。缺市场、低于观察阈值、货币步进错误、状态陈旧、包缺失、已有/已提交价格或冲突折扣会阻断；真实目录/最低阈值、收款 Partner、权限、商业判断和 Steamworks preview 全部 pending，CSV/Valve 提交/批准/价格发布/折扣/授权固定为 false。四页 proposal-only Collector 持续观察。
- 实验准入 `assistant.reconcile-current-work-projection`：直接组合固定 production Personal Knowledge Base，以 owner-bound 当前 Session 为排除项，对最近持久 Session 的各自 cursor 后增量按旧到新串行维护 current。隔离 probe 真实处理两个 Session、写 current、推进两个 cursor；精确重放零模型调用；第二个 Session 中断后保留第一个已提交状态，续跑只处理剩余 Session。Session 历史、durable Markdown 和 Git HEAD 未改，长期知识候选只留 proposal。公共结果不含正文/cwd/cursor 数值/model route/真实路径，并固定声明最近 12 个上限、枚举不完整、来源失败不可完全观察、`fullProjectionRebuild=false`、`cursorReset=false`。6-source proposal-only Collector 持续观察 production seam 与 E2E evidence。
- 准入 `steam.prepare-initial-release-date-review-revision`：冻结精确拟定后台日期、玩家侧 exact/month/quarter/year/Coming Soon 显示、store/build revision、24 小时内调用方观察状态、Coming Soon 公开日期和决策证据；派生可见范围与 Upcoming 排位日期，不生成本地化展示文案。目标非未来、Coming Soon 不足 14 天、store/build 未 Ready、状态已发布/陈旧/未来或两周锁定窗口内改精确日期会阻断。观察状态不被认证，所有权限与运营检查保持 pending；保存、Coming Soon 变更、Release App、上线、愿望单通知和授权固定为 false。Steam Release Dates、Coming Soon、Release Process 三页由 proposal-only Collector 持续观察。
- 实验准入 `feedback.expire-consented-intake-record`：准确绑定 storage receipt、record digest、原始 retention policy/deadline、可信 grant 与幂等键；deadline 前不调用 grant、不创建控制状态，deadline 后仍要求 `disposition=delete`、`holdStatus=clear`。隐藏库存只返回无正文 receipt/digest/policy/deadline，Collector 对 due 项只生成 proposal。隔离 probe 用 deadline 后一秒的固定时钟执行真实 unlink/sync，验证并发、post-unlink 恢复、active hold、篡改和路径拒绝。回执只证明 retention-expiry 逻辑删除，不冒充用户撤回、法律合规、media/backup/downstream 清除或生产调度。W3C、ICO、Node.js、NIST 四源 Maintainer 持续观察。
- 实验准入 `feedback.withdraw-consented-intake-record`：以既有 storage receipt、record digest、撤回请求/机制、可信 grant 和幂等键为唯一公共目标；隐藏 Connector 校验私有 envelope 和精确 capability/effect/store/receipt/digest/request/mechanism 授权。隔离 probe 真实创建上游反馈记录，使用 durable pending journal 删除并同步记录，提交独立 withdrawal receipt；准确并发只留一个 journal，两种中断窗口可恢复，伪授权、路径注入、篡改与变更请求失败。`logicalDeletionApplied=true`，但 media sanitization、backup purge、downstream-copy deletion、回复/平台/知识写入固定为 false。Node.js、W3C 与 NIST SP 800-88r2 三源 proposal-only Collector 持续观察。
- 实验准入 `feedback.persist-consented-intake-revision`：调用者只提交 opaque store/revision/grant refs、准确 revision hash 与幂等键；隐藏 Connector 重新验证 canonical intake revision，并要求可信 review grant 精确绑定 capability、`local-write`、store 和 revision。隔离 probe 实际以 `0600` 写入、同步、读回并清理私有 envelope，验证准确/并发重放只留一个记录，换 revision/幂等键、伪授权、篡改和路径注入均失败。回执只证明 `stored=true`，撤回/删除/回复/平台写入/知识写入/执行授权固定为 false；生产人审 UI/授权签发器尚未接入。Node.js 文件系统语义与 W3C purpose/retention/withdrawal 边界由 proposal-only Collector 持续观察。
- 准入 `feedback.prepare-consented-intake-review-revision`：把自有产品表单、支持表单、访谈或用户研究的一条已审阅 submission，与 scope/product/form/notice revision、decision、purpose、逐字段正文与摘要、consent evidence、其他人员数据声明、隐私审阅、保留期限及撤回机制冻结为稳定 hash。撤回/过期 consent、通知不匹配、越界用途、缺失/额外字段、身份/敏感信息未解决和超期保留会阻断；不自动检测 PII、不认证法律合规，存储/回执/撤回/回复/知识写入/授权固定为 false。W3C 与 OWASP 三源 proposal-only Collector 持续观察。
- 准入 `steam.prepare-early-access-review-revision`：冻结六项公开 Early Access Q&A、当前可玩 build/预告片/功能/限制、开发与资金资格、非绑定未来计划、社区影响、价格 revision/跨服务比较和第三方 Steam key 站点披露。漏答、不可玩/未知 build、已完成开发、依赖销量完成、具体未来保证、无社区影响、Steam 更高价或披露证据缺失会阻断；平台字段、实际价值和答案真实性全部 pending，保存/发布/送审/Early Access 上线/授权固定为 false。官方同页对计划 1.0 日期字段存在冲突说明，能力不暴露该字段；三页 proposal-only Collector 持续观察。
- 准入 `steam.prepare-content-survey-review-revision`：以调用者观察到的 questionnaire revision 和三部分完整问题集合为边界，绑定全部答案、逐题内容/证据、当前 build/store 一致性、所有已上传 build 成人内容披露，以及 pre/live-generated AI 的内容、权利和 guardrail 证据；漏答、额外/重复答案、缺部分、未确认声明及 AI 证据冲突直接阻断。具体问卷不复制为静态 Schema，当前映射、答案真实性、合法性和提交者权限全部 pending；问卷提交、评级、地区可见性、送审、发布和授权固定为 false。三处 Steam 官方规则由 proposal-only Collector 持续观察。
- 准入 `steam.prepare-supported-feature-review-revision`：把自有游戏、设计源、当前 build、观察到的 feature catalog 与逐功能实现/测试证据冻结为内容寻址待审 revision；planned、unknown、重复 ref/名称直接阻断。当前平台目录、证据质量、真实 public build 与实际可用性固定 pending；保存、预览、发布、送审、上线和授权全部为 false。两处 Steam 官方规则由 proposal-only Collector 持续观察。
- 实验准入 `assistant.evaluate-persona-continuity-suite`：冻结 persona revision 与七类候选响应情境，由两个带版本 evaluator 分别判断 role、boundary、value、style 和独立 system truth；保留 unknown 与 evaluator disagreement，不生成 companion-quality 总分，不声称 judge 独立。输入原文不进入结果，persona/memory/platform/action/authorization 均无副作用。当前 scripted evaluator 只验证契约，真实 Agent 判断、多语言稳定性和长期陪伴效果仍是 L3 缺口；三源 proposal-only Collector 持续观察实现语义和 ANCHOR 反证。
- 扩展“证据化调研”到首个 `market-competitive` 真实运行：三个 Apple US/iPhone 公开目录小页只作为可变 metadata/genre 候选信号；pet-mochi 与 YuriOS 固定源码证明 persona revision、短回复、hard limits 和封闭行为词表等机制存在；ANCHOR 长程审计反证“机制即连续性”，要求 persona continuity 与 trajectory recall 分开、记录 evaluator provenance 且不建 companion quality 总分。下一 effect-free probe 收敛为逐轴 role/boundary/value/style continuity eval；重复率和长期陪伴保持独立。Maintainer 新增三个 main 文件精确摘要，26 个来源当前无 proposal。
- 准入 `distribution.evaluate-impact-observation-set`：六组 Steam/Apple/Google Play fixture 验证同 native definition/surface/scope/unit、等长完整最终化窗口才计算 delta；Apple threshold suppression 不变零、报告未成熟保持 pending、Google Play 2026-07 定义变化单列 drift、Not attributed 保持 unknown。平台归因绑定 PublicationReceipt/evidence，时间关联明确不建立因果；跨平台比较、平台读取、知识写入、动作和授权全部禁止。四源 proposal-only Collector 当前通过。
- 扩展“证据化调研”到首个 `distribution-impact` 真实运行：核对 Steam UTM、App Store campaign/Analytics Reports 与 Google Play 2026-07 store-listing 指标语义，保留 finalization、阈值抑制、Not attributed 和 surface exclusion；拒绝跨平台影响力总分及简单前后因果，选择 effect-free、source-native Distribution Impact Observation evaluator 作为下一独立 probe。Maintainer 扩为 23 个 proposal-only 来源。
- 准入 `assistant.evaluate-versioned-memory-use-suite`：用十类冻结 fixture 和显式 supersession/revocation/contest/expiry/scope 图，分别验证 ingestion、retrieval、版本与 scope 解析、recall abstention/action ask 和 evidence-grounded utilization。输入输出只含 opaque refs 与摘要；不连接、读取或改写 memory backend，不把通过 fixture 冒充端到端个人助理可用。四篇固定版本论文由 proposal-only Collector 持续观察。
- 扩展“证据化调研”到首个 `academic-frontier` 真实运行：固定 EverMemBench v3、LongMemEval v2、Mem2ActBench v1、IFCMemoryBench v1 的版本与章节/表格，交叉纵向陪伴反证和本地已准入记忆切片；选择 backend-neutral、版本感知、可 abstain、分离 ingestion/retrieval/utilization 的 memory-use eval，不从 benchmark 排行直接选择生产后端，也不把记忆分数冒充陪伴结果。19-source proposal-only Collector 当前全部通过。
- 准入“Steam 逐 OS 系统要求 → 待人审 Revision”本地闭环：Windows、macOS、Linux/SteamOS 分别绑定 build artifact、depot、public package、启动测试与逐字段证据；最低核心字段必须完整，推荐层可缺省但不能半填，DirectX 只允许 Windows。实际最低可运行配置、性能目标和平台一致性固定 pending，保存/预览/发布/送审/上线/授权固定为 false；三处官方/第一方来源由 proposal-only Collector 持续观察。
- 实验准入“当前 Session 增量 → current projection 维护”本地闭环：固定生产 KnowledgeMaintainer 在隔离 Git 仓库真实读取未消费事件、原子更新 `.pkb/current.md`、推进 cursor 并生成一个未确认 proposal；同一 Session 无新事件重放不再次调用模型。公共面不接收或返回 transcript/cwd/cursor/model route/生成 Markdown；proposal 未 apply、durable knowledge 未写、Git HEAD 未变、执行未授权。6-source proposal-only Collector 持续观察生产维护逻辑、local-write 语义、layout 与 E2E evidence。
- 准入“读取有界工作上下文”本地闭环：隐藏 Connector 调用固定生产 Personal Knowledge Base，在隔离 Git 仓库真实组合 current、相关先前 Session 片段与匹配长期知识；公共面只接受 opaque Session/Workspace 和字符预算，排除当前 Session 回声，隐藏真实 cwd/revision，结果仅瞬时保留、不写 Session/知识、不授权执行。6-source Collector 持续观察生产 projection、repository layout 与 E2E evidence，任何漂移只生成 proposal。
- 扩展“证据化调研”平台接入场景：对 Google、闲鱼、58 与 BOSS 相关需求来源执行一次真实路线裁决；DataForSEO 只推进到 sandbox，闲鱼只保留一页用户可见 proposal，58/BOSS direct 自动路线因目标平台授权边界暂停，岗位需求只推进不声称 BOSS coverage 的 Coresignal 多源候选。供应商付费访问、目标平台授权、身份/计费门与下一 probe 保持为不同事实。
- 准入“有序 Steam Tag → 待人审 Revision”本地闭环：固定官方至少 5、建议最多 20、前 5 清楚描述游戏及排序影响发现的规则；逐 Tag 绑定首发证据并绑定目录/audience revision，顺序或证据变化使 hash 失效。平台有效性、相关性、构建一致性和目标权限保持 pending，保存/发布/送审/上线/授权固定为 false。
- 准入“normalized 双工事件 → 可逆轮次动作计划”本地闭环：候选语音只触发可恢复降音，误触释放与 backchannel 恢复输出，只有确认 `take-turn` 才取消输出并打开用户轮次；拒绝 raw audio/transcript/Provider wire event，自身不控制播放。固定生产源码证明当前运行时仍在 `speech_started` 直接取消，因此本能力是可采用的策略原语，不冒充产品已集成。
- 个人助理 approval 第二次 `technical-solution` 调研审计了 DSH one-shot seam、固定 Web Host transport 源码和三个安全反例：seam 的 current-turn/fail-closed/审计语义保留为未来内部原语，但当前 Web answerer 全局暴露 pending request 且回答缺 owner/channel attestation，已有 self-approval、cross-session hijack 与 late-cancellation grant 复现；因此不创建 approval Connector，等待 owner-bound transport 与真实负向 sandbox probe。
- 建立 OKF 公共知识、隐藏 Connector/Collector 与 live/sandbox probe 准入边界。
- 准入 TikTok 已知公共视频 URL 的官方 oEmbed 最小描述：live fixture 验证无需账号/凭据，只保留 VideoID、canonical URL、标题和缩略图尺寸，丢弃作者、HTML、缩略图 URL、指标、评论和 raw payload。YouTube 官方 Data API video-only 关键词搜索已实现为非路由 candidate；Google Cloud project、API key 和 live quota probe 未批准。TikTok Research API 因 commercial eligibility 暂停，Display API、Content Posting API 与 OOMOL managed research 保持分离的 researching route。
- 准入小红书 3 条、抖音 2 条、GitHub 5 条、Hugging Face 1 条真实闭环。
- 小红书与抖音 Collector 使用 GitHub 搜索、tag、release、文件和 issue/PR 变化维护开源生态，但变化只生成 proposal。
- 依据产品目标清理 9 条无关闭环：npm、PyPI、Go Module、crates.io、Maven Central、NuGet.org、Docker Hub、OSV，以及只登记 Node.js Release Feed 的 Web Feed Reader。
- 新增价值门：可验证只是必要条件；知识必须直接服务需求发现、产品/模型研究、内容或 App 发布、分发增长、反馈或影响力复盘。
- 准入本地“证据化调研”实验能力：统一需求、市场/竞品、技术、学术、平台接入与传播/影响力六种场景，吸收五个固定 commit 的 MIT 上游方法，并以 Research Dossier 隔离 Connector 与来源复杂度。
- 准入 Steam 公开游戏评论页：使用文档化公开端点读取最多 20 条 `recent|updated` cursor page，删除作者身份，运行时正文仅瞬时返回，Git 验证快照只保存长度与摘要。
- 准入“公开状态 → 宠物行为”本地闭环：从固定的 `dsh-codex-pet` 生产提交吸收 running/idle baseline 与等待、完成、失败、助手生命周期 pulse；拒绝 transcript、prompt、工具参数和隐藏推理，未吸收未提交 Character C0 实验。
- 准入“记忆 → Action Candidate”本地闭环：只绑定 exact action/scope/field 的 active、未过期、用户确认或工具验证 claim；冲突、过期和 inferred 记忆保持 unresolved，结果固定 `executionAuthorized=false`。
- 准入“本地游戏构建 → Revision”准备闭环：以 canonical workspace 边界、常规文件、密钥文件名、入口点和大小预算做预检，生成逐文件 SHA-256 manifest 与稳定 revision hash；固定 `uploaded=false`、`executionAuthorized=false`，不把本地 ready 冒充平台上传或上线。
- 准入“反馈观察 → Mutation Set”本地闭环：仅消费 opaque item ref、摘要、显式生命周期与回复状态；区分新增、编辑、回复、隐藏/删除和重新出现，缺失一律 unresolved，checkpoint 只提议不推进。
- 实验准入“有界反馈证据 → Theme Evidence”：主题逐项回链支持与反例，保留冲突、缺口和未分配证据，频率固定为 sample-only，结果要求人审且不执行；scripted Agent 仅证明契约，真实 L3 质量尚未验证。
- 准入“Steam 评论页 → partial 反馈观察窗口”：recommendation ID 形成稳定 item ref，语义摘要支持编辑检测；resume cursor 不冒充全局高水位，缺失不推断删除，checkpoint 固定 hold。
- 准入 arXiv 公开预印本元数据搜索：固定官方 Metadata API、纯 phrase/category 小页、进程内单连接三秒 gate、metadata-only；offset 明确不是稳定 delta，论文摘要不冒充已验证结论。
- 准入“Steam 商店图像 → 待人审 Revision”本地闭环：按当前官方四种 base capsule 尺寸与至少五张 16:9/1920×1080 截图要求检查真实 PNG/JPEG 文件、流式 SHA-256 和稳定 revision；视觉内容、权利与首发一致性固定 pending，上传/送审/发布/授权固定为 false。
- 准入“Action Candidate → Impact Review Revision”本地闭环：把完整 scalar arguments、scope、精确 targets、data/audience/cost/reversibility/consequence evidence 与 expiry 绑定成稳定 hash；未 grounded 或缺少 effect-specific 声明时阻断，金融/凭据/不可逆保守升级，但不接受决策、不签发 token、不授权执行。
- 扩展“证据化调研”真实场景验证：一次 `platform-integration` Agent 运行同时核对 Appfigures 官方评论/许可/计费、现有候选实现及 AppFollow/AppTweak 反证，形成“保留 5-credit 首个 probe、许可/价格/身份未批准前不执行”的 Research Dossier；这只证明一个真实样本，不宣称六场景 L3 已完成。
- 扩展“证据化调研”到 `technical-solution` 真实运行：核对已验证的 memory grounding / action impact review、OpenAI Agents SDK 和 LangGraph approval/resume 契约及重放反证，确定个人助理下一切片优先做可信 approval，并把交互捕获、一次性授权消费、执行与 receipt 对账保持为独立事实。
- 扩展“证据化调研”到首个 `demand` 真实运行：打开语音打断、长工具重复执行、宠物 false-idle 的 GitHub 一手问题，交叉 Mem2ActBench、Instruct-FD、陪伴纵向研究与本地已验证切片；保留 approval 为即时缺口，把全双工 turn-policy eval 排为随后独立切片，不从便利样本推断频率、不重复 pet projection、不把长期陪伴冒充 local 能力。
- 准入“Steam 本地化纯文本商店描述 → 待审 Revision”：冻结 English fallback、当前 full-platform language code、short description / About This Game、翻译/首发功能/权利依据与稳定摘要；明显链接、markup、缺 fallback 和本地预算超限会阻断，七项语义审阅保持 pending，后台保存/发布/送审/授权固定为 false。
- 准入“持久记忆变更 → 待审 Revision”：单项 USER.md/knowledge/*.md upsert/delete 绑定 base/current/desired 内容摘要、完整 Markdown、来源与证据；local probe 加载生产 Personal Knowledge Base，在隔离临时 Git 仓库实际跑通 proposal、显式确认、原子写、commit、receipt，并验证并发冲突和幂等重放；公开能力自身不创建 proposal、不写入、不确认。
- 准入“主动联系候选 → 待审 Revision”：显式 owner opt-in/pause、IANA 时区静默窗口、近期互动、min-gap、每日/连续未回复上限、source-visible、dedupe、copy/surface/expiry 共同决定 eligible/suppressed/blocked；MIRA 的 AGPL 生产策略只作语义证据、不复制实现，Vellum 与 Android 用户控制边界持续维护；结果不创建消息、不触达、不授权。
- 准入 Apple 公开应用目录搜索：固定文档化但已归档的 Search API、storefront country、software entity 和最多 25 条元数据；live probe 对 US iPhone `ChatGPT` 返回 5 条并核对 App ID `6448311069`。顺序、结果数与评分不冒充榜单、市场规模或需求，描述/artwork/release notes 不保留；Google Play 公开搜索、公开竞品评论与两店自有评论继续作为不同授权路线。
